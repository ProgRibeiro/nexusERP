import { addReferenceToBudget } from "@/app/actions/constructionBudgetActions";

export function ReferencePicker({ budgetId, references }: { budgetId: string; references: Array<{ id: string; code: string; description: string; unit: string | null; unitPrice: number; base: string }> }) {
  return <section className="mt-5 rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><h2 className="font-bold">Catálogo oficial</h2><p className="mb-3 text-xs text-zinc-500">Itens de referência disponíveis para inclusão.</p><div className="max-h-80 space-y-2 overflow-auto">{references.slice(0, 20).map((ref) => <form action={addReferenceToBudget} key={ref.id} className="flex items-center justify-between gap-3 rounded-lg border p-2 text-xs"><input type="hidden" name="budgetId" value={budgetId}/><input type="hidden" name="referenceId" value={ref.id}/><span className="min-w-0 flex-1"><b>{ref.base} · {ref.code}</b> — {ref.description}<small className="ml-2 text-zinc-400">{ref.unit || "UN"} · R$ {ref.unitPrice.toFixed(2)}</small></span><button className="rounded bg-blue-600 px-2 py-1 font-bold text-white">Adicionar</button></form>)}</div></section>;
}
